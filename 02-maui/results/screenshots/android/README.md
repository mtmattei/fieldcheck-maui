# android screenshots — not captured

No implementation screenshots exist for this target. The benchmark ran in a Linux cloud container:

- Android: no emulator or device (Google SDK host blocked by network policy; no /dev/kvm for hardware acceleration).
- Windows: the WinUI target cannot be built or run on Linux.

Capture these on a Windows machine with an Android emulator (412×915 portrait, 1440×900 window) to complete H01–H06.
